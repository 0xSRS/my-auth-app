```
git clone git@github.com:0xSRS/my-auth-app.git
cd .\my-auth-app\
npm i
npm run install-all
```
make .env file with reference to .env.example and fill the environment variables in the backend folder

after that, in the root folder only

```
npm run dev
```

After login on the web page, token is stored in the local storage
